import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  wrapper: {
    alignItems: 'center',
    justifyContent: 'space-between',
    height: 80,
    width: 40,
  },
  icon: {
    width: 30,
    height: 30,
    resizeMode: 'contain'
  },
  text: {
    fontSize: 15,
  }
})

export {styles};
