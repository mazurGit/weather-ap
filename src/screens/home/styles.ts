import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  daySegment:{
    marginTop: 10,
  },
  wrapper: {
    paddingHorizontal: 20,
  },
  daysWrapper: {
    marginTop: 30,
  }
});

export {styles};
